/**
 * @fileoverview Import feature
 * Let the user import HTML code when she creates a new page
 *
 */
export default function openImport(editor: any, config: any): {
    run(_: any): void;
    stop(): void;
};
