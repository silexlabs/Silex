<select class="prodotype-force-size">
  <% for(var idx in data) { %>
    <option
      value="<%= data[idx].value %>"
      <% if(selectedIndex === idx) { %>selected<% } %>
    >
        <%= data[idx].label %>
    </option>
  <% } %>
</select>
