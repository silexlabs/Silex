name: Docker image

# Builds and publishes the silexlabs/silex-platform image (root Dockerfile) —
# the same image used by v3.silex.me and the CapRover one-click app. Single source of
# truth, replacing the legacy silexlabs/silex-platform repo autobuild.
# Requires repo/org secrets DOCKER_USERNAME / DOCKER_PASSWORD.

on:
  push:
    tags: ['v*']

jobs:
  publish:
    name: build and push
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Compute tags
        id: tags
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          # Moving tag: prereleases (canary/alpha/beta) must NOT advance `latest`.
          if [[ "$VERSION" == *-* ]]; then
            echo "moving=prerelease" >> "$GITHUB_OUTPUT"
          else
            echo "moving=latest" >> "$GITHUB_OUTPUT"
          fi

      - name: Build and push
        uses: docker/build-push-action@v6
        env:
          DOCKER_BUILD_RECORD_UPLOAD: false
        with:
          context: .
          file: Dockerfile
          push: true
          tags: |
            silexlabs/silex-platform:${{ steps.tags.outputs.version }}
            silexlabs/silex-platform:${{ steps.tags.outputs.moving }}
