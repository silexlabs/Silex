# pnpm: hoist deps to top-level node_modules so build tools that expect a flat
# layout work (sass --load-path node_modules resolves transitive deps like
# codemirror/grapick imported by grapesjs scss).
shamefully-hoist=true
